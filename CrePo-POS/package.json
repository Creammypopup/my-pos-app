{
  "name": "crepo-pos",
  "version": "1.0.0",
  "description": "Main control for CrePo POS client and server",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "author": "Chayaporn Sasiwimon",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
